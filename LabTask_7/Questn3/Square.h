using namespace std;
namespace Shapes{
    class Square
    {
    private:
       float length;
    public:
        Square();
        Square(double length);
        double GetLength();
        ~Square();
    };
    
};